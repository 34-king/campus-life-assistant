# 校园生活助手 - 技术报告

## 1. 项目概述

- **项目名称**：校园生活助手（Campus Life Assistant）
- **开发框架**：Flutter 3.41.9 + Dart 3.11.5
- **项目类型**：移动应用开发实践课程期末项目
- **作者**：魏豪（312320020230）
- **指导老师**：任建吉
- **学校**：河南理工大学 · 2026年春季

### 1.1 项目背景

当前高校校园信息分散在多个平台——课表在教务系统、食堂菜单在公众号、公告在官网、校园卡在专用APP。学生需要在多个应用间切换，使用成本高。本项目的目标是打造一款一站式校园生活助手，将课程、餐饮、公告、校园卡、成绩、校历等功能整合到一个应用中。

### 1.2 开发环境

| 工具 | 版本 |
|------|------|
| Flutter | 3.41.9 (stable) |
| Dart | 3.11.5 |
| Android SDK | API 36 |
| DevEco Studio | 6.1 |
| 开发工具 | Trae AI IDE / Cursor |

## 2. 技术架构

### 2.1 整体架构

```
┌─────────────────────────────────┐
│          Flutter UI             │
│  (12个页面 + Material Design 3) │
├─────────────────────────────────┤
│        Provider 状态管理层        │
│  FavoritesProvider + ThemeProvider│
├─────────────────────────────────┤
│           数据层                 │
│  MockData + SharedPreferences   │
│  + HTTP (wttr.in天气API)        │
├─────────────────────────────────┤
│         Android APK 输出         │
└─────────────────────────────────┘
```

### 2.2 技术选型理由

| 技术 | 选择理由 |
|------|---------|
| Flutter 3.x | 跨平台开发，一套代码同时支持Android/iOS/Web |
| Provider | 轻量级状态管理，适合中小型应用 |
| Material Design 3 | Google最新设计规范，UI美观统一 |
| SharedPreferences | 轻量本地持久化，适合收藏/主题等简单数据 |
| HTTP (wttr.in) | 免费实时天气API，无需API Key |

## 3. 核心功能实现

### 3.1 页面结构

| 页面 | 功能 | 实现方式 |
|------|------|---------|
| 启动页 | 启动动画，2秒后自动跳转 | AnimatedOpacity |
| 首页 | 天气卡片 + 今日课程 + 快捷入口 | Stack + IndexedStack |
| 课程表 | 周视图网格，10节课×7天 | 二维Grid布局，按课程时间着色 |
| 食堂菜单 | TabBar按食堂筛选，菜品收藏 | TabBar + ChangeNotifier |
| 校园公告 | 公告列表，点击展开详情，收藏 | 底部Sheet弹窗 |
| 校园卡 | 余额显示+交易流水 | 卡片式布局 |
| 成绩查询 | GPA统计+分数环形指示器 | 按学期筛选 |
| 校历 | 月视图日历，事件标记 | 6列网格 |
| 校园地图 | 12个主要建筑展示 | Grid列表 |
| 个人中心 | 个人信息+功能入口 | 菜单列表 |
| 我的收藏 | 菜品+公告收藏 | 双Tab切换 |
| 设置 | 深色模式+字体+通知 | AppStorage持久化 |

### 3.2 状态管理

使用 Provider 的 ChangeNotifier 模式：

- **FavoritesProvider**：管理菜品和公告的收藏状态，数据通过 SharedPreferences 持久化。
- **ThemeProvider**：管理深色/浅色主题切换，持久化到 SharedPreferences。

```dart
// Provider 注册（main.dart）
MultiProvider(
  providers: [
    ChangeNotifierProvider.value(value: favProvider),
    ChangeNotifierProvider.value(value: themeProvider),
  ],
  child: const CampusLifeApp(),
)
```

### 3.3 网络交互

首页天气卡片通过 wttr.in API 获取实时天气数据：

```dart
final url = Uri.parse('https://wttr.in/Jiaozuo?format=j1');
final resp = await http.get(url).timeout(const Duration(seconds: 5));
```

API 返回 JSON 格式数据，解析后显示温度和天气描述。

### 3.4 本地持久化

SharedPreferences 存储以下数据：
- `favorite_dishes`：收藏的菜品列表（JSON数组）
- `favorite_notices`：收藏的公告列表（JSON数组）
- `theme_mode`：主题模式（system/light/dark）
- `notifications`：通知开关
- `font_scale`：字体大小缩放

## 4. AI 协作

### 4.1 开发工具

全程使用 **Trae AI IDE** 和 **Cursor** 辅助开发，所有源文件均标注了 AI 生成与人工修改的痕迹：

```
// [AI-GEN] - AI 自动生成的代码
// [HUMAN]  - 人工修改或添加的代码
```

### 4.2 典型 Prompt 示例

| 作用 | Prompt |
|------|--------|
| 项目初始化 | "创建Flutter校园生活助手项目，包含首页、课程表、食堂菜单等页面" |
| 状态管理 | "添加Provider状态管理，实现深色模式切换和收藏功能" |
| UI优化 | "优化为Material Design 3风格，使用colorSchemeSeed设置主题色" |
| 天气功能 | "添加实时天气模块，使用wttr.in API获取焦作天气数据" |
| 课程表 | "实现周视图课程表，按节次和星期排列，彩色课程卡片" |
| 校园卡 | "添加校园卡页面，显示余额和交易流水" |
| 成绩 | "实现成绩查询页面，支持按学期查看，GPA计算" |
| 校历 | "添加校历月视图，事件标记，支持事件类型分类" |

### 4.3 协作数据

- 总 Prompt 次数：19+
- 代码迭代轮数：17轮
- 代码占比：AI 生成约85%，人工修改约15%

## 5. HarmonyOS NEXT 适配

### 5.1 适配方案

提供两种适配方案：

**方案A：ArkTS 原生重写**
- 使用 DevEco Studio 6.1 + HarmonyOS SDK API 24
- 将 Flutter 页面逐一转换为 ArkTS + ArkUI 组件
- 已完成核心页面的 ArkTS 移植（E:\xyshzs 项目）
- 构建命令：`hvigorw assembleHap --mode module -p buildMode=debug`

**方案B：flutter-ohos 跨平台编译**
- 使用 OpenHarmony-SIG 的 flutter_flutter SDK (3.22.1-ohos)
- 直接编译 Flutter 代码为 HarmonyOS HAP 包
- 需配置 flutter-ohos SDK 环境变量

### 5.2 Flutter → ArkTS 映射

| Flutter | HarmonyOS |
|---------|-----------|
| Widget | @Component |
| State | @State |
| Provider | AppStorage |
| Navigator | router |
| SharedPreferences | AppStorage |

### 5.3 运行环境

- 模拟器：Pura 90 (phone, API 24)
- 系统：HarmonyOS 6.1.1 Beta1
- 适配文档：HARMONY_ADAPTATION.md

## 6. 工程规范

### 6.1 版本控制

- 使用 Git 进行版本控制
- 托管平台：GitHub（github.com/34-king/campus-life-assistant）
- 提交规范：遵循 Angular Commit Convention

### 6.2 代码结构

```
xyshzsapp/
├── lib/
│   ├── main.dart              # 应用入口 + 路由 + 导航
│   ├── models/
│   │   └── school_data.dart   # 数据模型 + 模拟数据
│   ├── providers/
│   │   ├── favorites_provider.dart  # 收藏状态管理
│   │   └── theme_provider.dart      # 主题切换
│   └── pages/                 # 12个功能页面
├── test/                      # 测试
└── pubspec.yaml               # 依赖配置
```

### 6.3 依赖管理

| 依赖 | 版本 | 用途 |
|------|------|------|
| flutter | 3.41.9 | UI框架 |
| provider | ^6.1.5 | 状态管理 |
| shared_preferences | ^2.5.5 | 本地持久化 |
| http | ^1.6.0 | 网络请求 |
| cupertino_icons | ^1.0.8 | iOS风格图标 |

## 7. 测试与运行

### 7.1 环境要求

- Flutter SDK 3.x
- Android SDK (API 21+)
- 支持平台：Android / Web / Windows

### 7.2 构建命令

```bash
flutter pub get           # 安装依赖
flutter run               # 运行
flutter build apk --debug  # 构建APK
```

### 7.3 运行截图

（此处插入应用运行截图）

## 8. 总结与展望

### 8.1 完成情况

- 12个功能页面（远超要求的3-5个）
- Material Design 3 UI 规范
- Provider 状态管理
- 本地数据持久化 + 网络数据交互
- 深色模式切换
- HarmonyOS NEXT 适配方案
- AI 协作开发全程标注

### 8.2 可改进方向

1. 接入真实教务系统API获取实时课表
2. 增加校园失物招领、二手交易等社交功能
3. 完善推送通知功能
4. 优化HarmonyOS适配，完成HAP构建
5. 增加单元测试和集成测试

---

**报告日期**：2026年6月  
**项目地址**：https://github.com/34-king/campus-life-assistant
